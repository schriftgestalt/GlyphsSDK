## Kern Table Subsetter

This is a sample tool that shows how to use the kern table API

1) install the plugin as usual
2) add a `Export kern Table` and `Mekkablue kern table subsetter` to an instance that should have a subsetted kern table
3) export as usual. 